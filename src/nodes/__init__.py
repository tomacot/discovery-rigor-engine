"""
Node package for the Discovery Rigor Engine LangGraph graph.

Each module in this package implements one node in the graph.
Nodes are plain functions: (StudyState) -> dict (partial state update).
"""
